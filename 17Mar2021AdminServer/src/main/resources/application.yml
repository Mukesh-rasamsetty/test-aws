server:
  port: 9999
spring:
  application:
    name: spring-boot-admin
  boot:
    admin:
      discovery:
        ignored-services: spring-boot-admin
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/